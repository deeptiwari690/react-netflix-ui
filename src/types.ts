export type ValuePropsItem = {
  title: string;
  description: string;
  icon: string;
};

export type Top10Item = {
  rank: number;
  poster: string;
  label: string;
};

export type FaqItem = {
  question: string;
  answer: string;
  id: string;
};

export type FooterLink = {
  href: string;
  label: string;
};
